const { Util, MessageEmbed } = require("discord.js");
const ytdl = require("ytdl-core");
const yts = require("yt-search");
module.exports = {
  info: {
    name: "play2",
    description: "To play songs :D",
  },

  run: async function (client, message, args) {
   if(args[2] == spam) {
    if(args[3] == '!{PlayerTag}'){
      message.player.private.player.PlayerTag('YEET !{PlayerTag} I am gonna steal your wife');
      if(player.private.messanger = send.message){
       if(args[0] == stop) {
          return(); 
       }
        else{
           message.player.private.player.PlayerTag('YEET !{PlayerTag} I am gonna steal your wife');
          loop();
       }
      }
    }
    else{
     message.channel.send('Wrong command IDOT'); 
    }
   }
    
    
  }
